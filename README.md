# LUMEX DoH Registry

Machine-readable encrypted resolver registry for LUMEX VPN clients.

The payload is refreshed automatically by GitHub Actions. Resolver addresses and validation internals are intentionally not published as plain text.
